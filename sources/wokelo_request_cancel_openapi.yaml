openapi: 3.0.3
info:
  title: Request Cancel
  description: Cancels a request already submitted.
  version: 2.0.0
  license:
    name: Proprietary
servers:
- url: https://api.wokelo.ai
paths:
  /api/enterprise/request/cancel:
    post:
      tags:
      - Supporting APIs
      summary: Request Cancel
      description: 'Source docs: request-cancel.md'
      operationId: post_api_enterprise_request_cancel
      responses:
        '200':
          description: Successful response
      x-source-variants:
      - group_path: Supporting APIs
        request_name: Request Cancel
      parameters:
      - name: request_id
        in: query
        required: true
        schema:
          type: string
        description: The unique identifier of request which needs to be cancelled
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                request_id:
                  type: string
              required:
              - request_id
              description: "[DERIVED FROM EXAMPLE \u2014 not authoritative] Request\
                \ schema from Postman raw body."
      security:
      - bearerAuth: []
tags:
- name: Supporting APIs
  description: Supporting endpoints for uploads, status, downloads, and configuration.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
